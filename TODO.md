# 📋 АКТУАЛЬНЫЙ ROADMAP (Flask + JS Frontend)

> _Обновлено: 20.04.2026 | Статус: 🟢 Этапы 1-2 завершены, оптимизация логирования и кэша — готова_

---

## ✅ ЭТАП 1: Минимальный рабочий вариант (MVP) — **ГОТОВО**

| Задача                           | Статус | Примечание                                  |
| -------------------------------- | :----: | ------------------------------------------- |
| 1.1 Базовый Flask-сервер `/`     |   ✅   | `app.py` + Jinja2                           |
| 1.2 HTML-форма                   |   ✅   | Semantic HTML, a11y                         |
| 1.3 POST `/api/generate`         |   ✅   | Интеграция с `llm/`, `checker/`, `cache.py` |
| 1.4 JS: отправка, лоадер, рендер |   ✅   | Модульный `app.js`, `fetch`, XSS-safe       |
| 1.5 Интеграция с модулями        |   ✅   | Централизованный `config.py`                |

---

## ✅ ЭТАП 2: Удобный интерфейс (UI/UX) — **ГОТОВО**

| Задача                                | Статус | Примечание                                        |
| ------------------------------------- | :----: | ------------------------------------------------- |
| 2.1 Тёмный UI + дизайн-система        |   ✅   | OKLCH, Fira Code, `ui.css` + `app.css`            |
| 2.2 Карточки с цветной полосой        |   ✅   | `border-left`, `slideIn`, hover                   |
| 2.3 Статистика (✅/❌/❓)             |   ✅   | 3 бейджа, динамический рендер                     |
| 2.4 Экспорт в JSON                    |   ✅   | Кнопка «💾», читаемая кириллица, `prompt` в файле |
| 2.5 Индикаторы статуса                |   ✅   | `⏳` → `✓`/`✗`, цветовая подсветка                |
| 2.6 Анимации, адаптив, чипсы, stepper |   ✅   | Grid, media queries, `tld-chip`                   |

---

## ✅ ЭТАП 2.5: Оптимизация и наблюдаемость — **ГОТОВО**

| Задача                                   | Статус | Что сделано                                                     |
| ---------------------------------------- | :----: | --------------------------------------------------------------- |
| 2.5.1 Кэш экземпляров провайдеров        |   ✅   | `_provider_cache` в `app.py`: конфиг грузится 1 раз за сессию   |
| 2.5.2 Единый стандарт логирования        |   ✅   | Префиксы `:CONFIG/:CONNECT/:MODEL/:GENERATE`, уровни по событию |
| 2.5.3 Разделение консоль/файл            |   ✅   | `INFO+` в консоль, `DEBUG+` в `logs/domain_finder.log`          |
| 2.5.4 Подавление «грязного» вывода WHOIS |   ✅   | `_SuppressStderr` + `_truncate_error()` в `whois.py`            |
| 2.5.5 Консервативная эвристика WHOIS     |   ✅   | Таймауты/неполные данные = `unknown`, а не `taken`              |

---

## 🔵 ЭТАП 3: Фоновая обработка (Async/Realtime) — **СЛЕДУЮЩИЙ ШАГ**

| Задача                     | Описание                                                       | Сложность |
| -------------------------- | -------------------------------------------------------------- | --------- |
| 3.1 Background Worker      | `ThreadPoolExecutor` или `asyncio` для неблокирующих WHOIS/LLM | ⭐⭐⭐    |
| 3.2 Task ID & State        | Генерация `uuid4`, хранение статуса в `session` или `tempfile` | ⭐⭐      |
| 3.3 SSE / Polling endpoint | `GET /api/status/<task_id>` → `{progress, status, result_url}` | ⭐⭐⭐    |
| 3.4 UI: Live Progress      | JS-подписка на SSE, плавное обновление прогресса               | ⭐⭐      |
| 3.5 Хранение результатов   | Временные JSON-файлы с TTL (автоочистка через 24ч)             | ⭐⭐      |

---

## 🟣 ЭТАП 4: Расширенные функции

| Задача                                      |   Статус   | Приоритет |
| ------------------------------------------- | :--------: | --------- |
| 4.1 Экспорт результатов в JSON              |  ✅ Done   | —         |
| 4.2 Фильтрация (только свободные / по зоне) | 📝 Planned | Medium    |
| 4.3 История запросов (localStorage)         | 📝 Planned | Low       |
| 4.4 «Избранное» + копирование в буфер       | 📝 Planned | Medium    |
| 4.5 Мультиязычность (RU/EN)                 | 📝 Planned | Low       |

---

## 🟠 ЭТАП 5: Продакшен и безопасность

| Задача                     | Текущее состояние | Что добавить                                        |
| -------------------------- | ----------------- | --------------------------------------------------- |
| 5.1 Rate Limiting          | ❌ Не реализовано | `Flask-Limiter` (5 req/min)                         |
| 5.2 Валидация + XSS защита | ✅ Частично       | `escapeHtml` в JS, `get_json(silent=True)` в Python |
| 5.3 Скрытие ключей         | ✅                | Ключи только в `.env` / `config`                    |
| 5.4 HTTPS + Deploy         | ❌                | `gunicorn` + `nginx`                                |
| 5.5 Общий кэш              | ❌                | In-memory → Redis при масштабировании               |

---

## 📦 СТРУКТУРА ПРОЕКТА

```bash
src/domain_finder/
├── web/
│   ├── app.py                  # Flask + API
│   ├── templates/index.html    # Jinja2-шаблон
│   └── static/
│       ├── css/                # ui.css, app.css, styles.css
│       └── js/app.js           # Клиентская логика
├── llm/
│   ├── provider_base.py
│   └── providers/
│       ├── openrouter_provider.py
│       └── ollama_provider.py
├── checker/whois.py            # WHOIS-проверка
├── cache.py                    # In-memory кэш с TTL
├── config.py                   # Настройки, модели, ключи
├── logger.py                   # Логгер с EmojiFormatter
└── main.py                     # CLI-точка входа
```

---

## 🛠️ Что дальше?

Проект полностью работоспособен. Следующие шаги — на ваше усмотрение:

1. **🔵 Этап 3** — асинхронная генерация + прогресс (улучшение UX при долгом ожидании)
2. **🟣 Этап 4.2** — фильтрация результатов (только свободные / по зоне)
3. **🟠 Этап 5.1** — Rate Limiting (защита от злоупотреблений)

**Что из этого сделать следующим?** Или есть другая задача — просто скажите, что добавить, и я сделаю. 🟣✨
